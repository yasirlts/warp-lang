{
  "name": "@warp-lang/commerce-mcp",
  "version": "0.1.0",
  "description": "Warp's commerce-integrity guardrail exposed as Model Context Protocol (MCP) tools — validate the structural coherence of a commerce action before it flows to payment authorization or checkout. A thin wrapper over the published @warp-lang/commerce-types; it validates and returns verdicts, it does not execute, authorize, settle, or hold credentials.",
  "type": "module",
  "bin": {
    "warp-commerce-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js",
    "example": "node examples/over-refund.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@warp-lang/commerce-types": "^1.4.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/yasirlts/warp-lang",
    "directory": "packages/commerce-mcp"
  }
}
