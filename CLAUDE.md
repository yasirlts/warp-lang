@AGENTS.md

# Warp — Agent Rules

The agent rules for this repository live in **[AGENTS.md](AGENTS.md)**, the
cross-tool canonical guide read natively by most agent CLIs.

This file exists only because Claude Code loads `CLAUDE.md` by filename; the first
line above imports `AGENTS.md` so there is a single source of truth and the two
cannot drift. Do not add rules here — edit [AGENTS.md](AGENTS.md) instead.
